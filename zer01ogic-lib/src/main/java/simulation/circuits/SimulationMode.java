package simulation.circuits;

/**
 * Modes that simulation can run in
 * TICK - pause after each logic element update
 * NONSTOP - standard simulation mode
 */
public enum SimulationMode {
    TICK,
    NONSTOP
}
